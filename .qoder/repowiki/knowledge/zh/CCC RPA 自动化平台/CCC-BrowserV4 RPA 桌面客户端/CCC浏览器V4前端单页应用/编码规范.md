- 所有 API 模块（auth, tasks, execution）均导入并复用 `src/api/request.ts` 中定义的 Axios 实例，而非各自创建。
- 路由配置采用懒加载模式（`() => import(...)`），并在元信息中通过 `requiresAuth` 标记鉴权需求，由全局守卫统一处理。
- Tauri 原生能力调用统一收敛至 `src/utils/tauri-bridge.ts`，业务模块通过该桥接对象间接 invoke，避免直接依赖 `@tauri-apps/api`。