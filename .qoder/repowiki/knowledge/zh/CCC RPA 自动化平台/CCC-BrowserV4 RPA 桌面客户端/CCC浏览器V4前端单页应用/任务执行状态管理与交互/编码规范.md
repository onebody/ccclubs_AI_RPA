- 采用状态机模式管理任务流程，通过字符串字面量（如 'qr_scanning', 'waiting_company'）明确界定执行阶段。
- 实现演示模式（Demo Mode）逻辑，在 API 调用失败或手动触发时，通过 `setTimeout` 模拟异步流程和生成 Mock 数据。
- UI 组件与业务逻辑解耦，视图层仅负责根据 Store 状态渲染界面，所有状态变更和业务判断均下沉至 Store。