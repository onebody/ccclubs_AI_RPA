- 采用 Store 模式封装业务逻辑，组件通过调用 Store 方法（如 `loadTasks`, `executeTaskById`）间接与 API 交互，实现视图与数据获取解耦。
- 在列表页（TaskPage）使用 `onMounted` 初始化 WebSocket 连接，并在 `onUnmounted` 中销毁，确保实时状态同步的生命周期管理。
- 任务状态（pending/running/completed/failed）在前端通过统一的映射函数（如 `statusLabel`, `statusTagType`）转换为可读文本和 UI 样式。