- 入口层：`main.ts` 集成 Pinia、Vue Router 及 Element Plus，完成全局插件注册与样式注入。
- 路由层：`router/index.ts` 采用基于 `meta.requiresAuth` 的声明式鉴权守卫，实现登录态自动重定向与页面保护。
- 桥接层：`utils/tauri-bridge.ts` 封装 Tauri `invoke` 调用，提供设备ID获取、Token生成及本地回调服务器启动等原生能力接口。
- 根组件：`App.vue` 作为布局容器，在挂载阶段触发全局状态（Auth/Device）的初始化与持久化恢复。