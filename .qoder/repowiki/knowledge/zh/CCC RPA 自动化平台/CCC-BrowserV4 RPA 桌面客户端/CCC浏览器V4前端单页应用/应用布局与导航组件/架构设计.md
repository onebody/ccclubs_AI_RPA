- `AppLayout.vue` 作为根容器，使用 `el-container` 组合 `SideMenu`（侧边栏）和 `StatusBar`（底部状态栏），中间嵌入 `router-view` 渲染动态页面。
- `SideMenu.vue` 负责导航逻辑，通过 `vue-router` 实现路由跳转，并动态高亮当前激活菜单项。
- `StatusBar.vue` 展示全局状态信息，直接依赖 `useAuthStore` 和 `useDeviceStore` 获取用户登录状态及设备 ID，体现布局层对全局状态的直接消费。