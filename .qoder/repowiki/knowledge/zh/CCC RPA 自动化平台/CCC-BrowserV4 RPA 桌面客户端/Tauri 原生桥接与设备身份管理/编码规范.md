- 所有暴露给前端的接口均定义为 `pub async fn` 并添加 `#[tauri::command]` 宏，统一返回 `Result<T, String>` 类型以规范错误处理。
- 涉及本地状态管理的逻辑（如设备 ID）统一封装在独立的子模块（如 `device.rs`）中，通过 `StoreBuilder` 访问 `device.json` 文件实现持久化。
- 耗时或阻塞型操作（如监听 HTTP 回调）通过 `thread::spawn` 放入后台线程执行，避免阻塞 Tauri 主事件循环，并通过 `Emitter` 接口向前端发送异步事件通知。