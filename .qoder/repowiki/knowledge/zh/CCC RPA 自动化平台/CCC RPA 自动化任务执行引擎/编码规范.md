- 所有涉及 Playwright 页面操作的方法均通过 `BrowserSessionManager.run()` 提交至专用工作线程执行，严禁在 API 或 Service 层直接调用 Playwright 实例。
- 自动化脚本中广泛采用多级降级选择器策略（如 `scrape_company_list`），依次尝试 CSS 类名、通用列表结构及文本正则匹配，以增强对目标站点 UI 变动的鲁棒性。
- 关键业务流程（如登录、保活）中集成 `HumanBehavior` 模块，模拟随机滚动、非均匀延迟及鼠标轨迹，以降低被反爬虫系统识别的风险。