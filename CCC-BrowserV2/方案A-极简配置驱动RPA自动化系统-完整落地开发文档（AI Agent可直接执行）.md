# 方案A\-极简配置驱动RPA自动化系统\-完整落地开发文档（AI Agent可直接执行）

## 0\. 最终确认：本系统核心业务定位（AI Agent必须严格遵守）

**核心业务链路（唯一业务模型）：**

首次沙箱登录 → 会话缓存复用（免重复登录） → 切换场景1/场景2站内页面 → 调用后端业务API批量获取多用户数据 → 循环自动填表单\+提交 → 任务结束沙箱销毁/保活

**核心特性（固定不变）：**

- 流程**完全配置化**，不做任何录制、不做Chrome扩展、不做拖拽RPA编辑器

- 支持 **一次登录、全程复用会话**

- 支持 **多场景页面模板切换**（场景1、场景2可无限扩展）

- 所有批量数据来自 **后端业务API**（预留标准化调用入口）

- 全浏览器真人仿真、WZWS全维度绕过、多进程隔离、防关联

- 全功能 Web 可视化（配置、调度、授权、监控、日志）

- 支持 Windows / macOS / Ubuntu 三端桌面客户端打包运行

**彻底砍掉（禁止开发）：**

- ❌ Chrome 扩展 / SidePanel

- ❌ 页面操作录制功能

- ❌ 可视化拖拽流程画布

- ❌ 前端实时画面流推送

## 1\. 整体架构定型（AI Agent 严格遵循）

**四层极简架构**

```plain
Web可视化管理后台（唯一操作入口）
        ↓
本地桌面调度服务（跨三端）
        ↓
Playwright+独立Chromium多沙箱引擎（防风控、仿真、隔离）
        ↓
外部用户业务API（预留通用调用接口，批量取数）
```

## 2\. 最终技术栈定型 \& 全套技术约束规范（不可改动、AI Agent严格强制执行）

本章为系统**唯一法定技术栈、版本约束、编译规则、依赖规范、兼容规则**，所有开发必须严格遵守，禁止私自替换框架、升级版本、新增冗余依赖、修改编译参数，保障三端一致性、系统稳定性、防风控统一性。

### 2\.1 整体技术栈全景清单（固定不可变更）

- **桌面客户端壳（跨三端）**：Tauri v2 稳定版

- **自动化内核引擎**：Playwright\-Core 1\.45\.x 固定版本 \+ 定制化三端Chromium内核

- **后端服务框架**：NestJS（Node\.js 20\.x LTS 固定版本）

- **前端可视化后台**：Vue3 \+ Vite \+ Element Plus 最新稳定版

- **任务队列中间件**：BullMQ \+ Redis 7\.0\.x 固定版本

- **关系型数据库**：PostgreSQL 15\.x 固定版本

- **缓存/会话存储**：Redis 7\.0\.x（本地单机部署，不使用集群）

- **鉴权加密工具**：JWT \+ AES\-256\-GCM（租户数据加密固定算法）

- **日志处理**：Winston 结构化日志（本地文件\+数据库双存储）

- **跨端打包工具**：Tauri Cli 官方打包，禁止第三方打包工具

### 2\.2 各组件详细选型理由 \+ 强制技术约束（核心落地规则）

#### 2\.2\.1 Tauri 桌面端约束（替代Electron，轻量无冗余）

- **选型理由**：体积小、内存占用极低、无内置冗余Chromium、三端打包一致性高，适合批量沙箱并发场景，规避Electron内存泄露问题

- **强制约束**：
  固定 Tauri v2 稳定分支，禁止使用Beta/开发版

- 禁止开启Tauri内置浏览器，所有自动化浏览器统一使用项目内置独立Chromium

- 所有本地服务、接口、调度服务**仅本地127\.0\.0\.1监听**，禁止外网暴露

- 三端打包必须支持：Windows exe、macOS dmg、Ubuntu deb，架构全覆盖

- 禁止集成任何Chrome扩展、网页插件、第三方脚本能力

#### 2\.2\.2 Playwright\-Core \+ 定制Chromium 防风控内核约束（WZWS绕过核心）

- **选型理由**：无原生webdriver强特征、CDP可控性高、支持进程级沙箱隔离、适配定制内核，是当前稳定绕过WZWS的最优轻量化内核

- **强制约束**：
  固定 Playwright\-Core 版本，禁止自动升级（版本变更会导致CDP特征、指纹特征偏移，风控失效）

- 必须使用**项目内置定制去特征Chromium**，禁止调用系统自带Chrome/Chromium

- 定制内核强制删减：cdc\_全局变量、automation自动化标记、远程调试特征残留

- 所有浏览器启动参数统一固化，禁止动态增减启动参数

- 严格隔离：一任务一进程、一进程一userDataDir、一进程一代理IP，禁止会话混用

- 无头模式固定使用 `headless: "new"`，禁止旧版无头模式（特征极强易被拦截）

#### 2\.2\.3 NestJS 后端服务约束

- **选型理由**：模块化清晰、依赖注入规范、适合任务队列\+多租户权限体系、长期可维护性高，适配Playwright异步调度模型

- **强制约束**：
  固定Node\.js 20\.x LTS，禁止使用16/18/22等其他版本

- 全局统一异常拦截、统一响应格式，所有接口返回结构标准化

- 禁止在业务层写死任何页面元素、URL、表单字段，全部走数据库配置解析

- 所有沙箱进程、任务队列、会话缓存统一托管，禁止孤儿进程、僵尸任务

- 接口仅对内网/本地提供，禁止开放公网访问，所有外部调用需租户Token鉴权

#### 2\.2\.4 Vue3\+Element Plus 可视化后台约束

- **选型理由**：组件成熟、后台可视化适配度高、开发效率快，满足全配置、全监控、全授权可视化需求

- **强制约束**：
  仅做**配置、展示、调度、操作指令下发**，不承载任何自动化执行逻辑

- 禁止开发页面录制、拖拽画布、实时流播放等冗余功能

- 所有表单配置项、模板配置项**参数校验前置**，非法参数禁止入库

- 敏感数据（租户密钥、代理信息、API密钥）前台一律脱敏展示

#### 2\.2\.5 BullMQ\+Redis 任务调度约束

- **选型理由**：支持延迟任务、定时任务、失败重试、并发控制、任务状态持久化，完美适配批量循环自动化场景

- **强制约束**：
  Redis仅本地单机部署，禁止集群、禁止外网连接

- 所有自动化任务统一入队，禁止直接执行异步任务

- 严格按照租户并发配额限流，超额任务排队不丢弃

- 任务失败重试次数、重试间隔全局可配置，禁止代码写死

#### 2\.2\.6 PostgreSQL 数据库约束

- **选型理由**：稳定、支持事务、适合多租户结构化数据、任务日志、权限数据持久化

- **强制约束**：所有数据表**强制携带tenant\_id**，严格租户数据隔离

- 流程模板、API配置、任务记录禁止物理删除，仅逻辑删除

- 敏感字段加密存储，禁止明文存储账号、密钥、接口凭证

### 2\.3 全局统一技术开发约束（AI Agent 强制执行）

- **解耦约束**：业务数据、页面元素、流程规则、调度参数**全部配置化**，零硬编码，无需改代码即可新增场景、适配新表单，彻底解耦业务与自动化内核

- **防风控约束**：所有自动化执行必须叠加全套仿真\&指纹抹平能力，禁止裸机原生Playwright执行，所有浏览器实例必须加载内置防爬配置与真人行为逻辑

- **跨端统一约束**：三端Windows/macOS/Ubuntu执行逻辑、延时规则、指纹参数、流程解析规则100%一致，无差异化打包、无平台专属特殊逻辑

- **性能约束**：单进程内存可控，批量并发无内存泄露，任务结束强制销毁进程、清空缓存、回收系统资源，杜绝孤儿进程、僵尸任务

- **安全约束**：所有本地服务仅127\.0\.0\.1本地监听，禁止外网暴露；租户数据严格隔离、接口全局鉴权、敏感数据脱敏、关键字段加密存储全覆盖

- **极简约束**：禁止引入任何无用依赖、冗余功能、录制模块、扩展模块，保持架构轻量化、高可用、易运维

- **规范约束**：所有代码遵循模块化、分层开发规范，前后端分离，配置与逻辑分离，业务与内核分离

- **解耦约束**：业务数据、页面元素、流程规则、调度参数**全部配置化**，零硬编码，无需改代码即可新增场景、适配新表单

- **防风控约束**：所有自动化执行必须叠加全套仿真\&指纹抹平能力，禁止裸机原生Playwright执行

- **跨端统一约束**：三端Windows/macOS/Ubuntu执行逻辑、延时规则、指纹参数、流程解析规则100%一致，无差异化

- **性能约束**：单进程内存可控，批量并发无内存泄露，任务结束资源强制回收

- **安全约束**：所有本地服务仅127\.0\.0\.1访问，租户数据隔离、接口鉴权、数据脱敏、加密存储全覆盖

- **极简约束**：禁止引入任何无用依赖、冗余功能、录制模块、扩展模块，保持架构轻量化

### 2\.4 版本锁定与依赖管理规范（强制落地）

为杜绝版本迭代导致的风控特征变异、程序兼容报错、三端运行不一致问题，所有核心依赖**强制固定版本，禁止模糊匹配与自动升级**，具体规范如下：

- 核心框架、中间件全部写死具体版本号，禁止使用 ^、\~ 模糊版本匹配符号

- 项目必须生成并锁定 package\-lock\.json / yarn\.lock 文件，所有开发、打包、部署环境强制同步依赖版本

- 三端开发环境统一镜像源、统一依赖版本，杜绝局部环境差异导致的运行异常

- 禁止私自升级 Playwright、Chromium、Redis、Node\.js 等风控相关、内核相关依赖版本

### 2\.5 核心依赖精准版本锁定清单（强制不可变更）

所有依赖严格锁定具体版本，无模糊匹配，三端开发/打包/部署环境完全统一，杜绝版本差异导致的风控失效、程序报错、兼容问题。

#### 2\.5\.1 基础运行环境版本

- Node\.js：20\.12\.2 LTS（固定版本，禁止升降级）

- npm：10\.5\.0（配套Node版本，统一包管理）

- Redis：7\.0\.15（本地单机部署，固定版本）

- PostgreSQL：15\.8（固定数据库版本）

#### 2\.5\.2 核心框架与内核版本

- Tauri：2\.0\.0\-beta\.28（稳定版，固定桌面壳版本）

- Playwright\-Core：1\.45\.3（风控核心版本，禁止升级）

- NestJS 核心包：@nestjs/core@10\.3\.0、@nestjs/common@10\.3\.0、@nestjs/platform\-express@10\.3\.0

- Vue3：3\.4\.15 \+ Vite：5\.1\.0

- Element Plus：2\.4\.4

- BullMQ：5\.12\.0（任务队列固定版本）

#### 2\.5\.3 工具类依赖固定版本

- winston：3\.11\.0（结构化日志）

- jsonwebtoken：9\.0\.2（JWT鉴权）

- crypto\-js：4\.2\.0（AES\-256\-GCM加密）

- pg：8\.11\.3（PostgreSQL驱动）

- ioredis：5\.3\.2（Redis客户端）

### 2\.6 统一环境初始化脚本（AI Agent 直接执行）

提供跨三端通用初始化脚本，一键搭建标准化开发/运行环境，保证所有运行环境一致性。

#### 2\.6\.1 全局环境初始化命令

```shell
# 1. 锁定Node版本（三端统一）
nvm use 20.12.2
npm config set registry https://registry.npmmirror.com/

# 2. 全局安装固定依赖
npm install -g @tauri-apps/cli@2.0.0-beta.28
npm install -g playwright@1.45.3

# 3. 项目依赖安装（严格锁定lock文件）
npm ci

# 4. 初始化数据库与Redis
# 手动初始化PostgreSQL15.8、Redis7.0.15本地单机服务，禁止外网映射

```

#### 2\.6\.2 项目依赖锁定配置（package\.json 强制写入）

```json
{
  "engines": {
    "node": "20.12.2",
    "npm": "10.5.0"
  },
  "dependencies": {
    "@nestjs/common": "10.3.0",
    "@nestjs/core": "10.3.0",
    "@nestjs/platform-express": "10.3.0",
    "playwright-core": "1.45.3",
    "bullmq": "5.12.0",
    "ioredis": "5.3.2",
    "pg": "8.11.3",
    "winston": "3.11.0",
    "jsonwebtoken": "9.0.2",
    "crypto-js": "4.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.0.0-beta.28",
    "vite": "5.1.0",
    "vue": "3.4.15",
    "element-plus": "2.4.4"
  }
}
```

### 2\.7 三端统一编译与打包规范

- **打包唯一入口**：仅使用 `tauri build` 官方命令，禁止自定义打包脚本、第三方打包工具

- **三端打包产物规范**：Windows输出exe安装包、macOS输出dmg镜像包、Ubuntu输出deb安装包

- **编译强制规则**：打包前自动清空冗余缓存、校验依赖版本、校验Chromium内核完整性，版本不匹配禁止打包

- **运行环境规范**：所有客户端运行时仅读取本地runtime内核、本地数据库、本地Redis，无外网资源依赖

- **版本固化规则**：打包产物内置版本信息、依赖版本指纹，防止环境篡改

### 2\.8 精简汇总：核心技术栈极简清单（备用查阅）

- **桌面端壳**：Tauri v2\.0\.0\-beta\.28（跨三端轻量化客户端）

- **自动化内核**：Playwright\-Core 1\.45\.3 \+ 定制去特征三端Chromium

- **后端服务框架**：NestJS \+ Node\.js 20\.12\.2 LTS

- **前端可视化后台**：Vue3 3\.4\.15 \+ Vite 5\.1\.0 \+ Element Plus 2\.4\.4

- **任务调度中间件**：BullMQ 5\.12\.0 \+ Redis 7\.0\.15（本地单机）

- **数据持久化**：PostgreSQL 15\.8

- **加密与日志**：AES\-256\-GCM加密、JWT 9\.0\.2鉴权、Winston 3\.11\.0结构化日志

- **打包工具**：官方Tauri Cli（唯一打包入口）

## 3\. 统一项目目录结构（AI Agent 必须按此创建）

```plain
/
├── src-tauri/                # 三端桌面壳
├── backend/                  # 调度服务、RPA引擎、队列、数据库
│   ├── engine/               # Playwright沙箱引擎、仿真、WZWS绕过
│   ├── process-config/       # 流程模板解析、多场景解析、会话复用
│   ├── task-queue/           # 批量任务调度、循环执行器
│   ├── api-client/           # 【重点预留】后端业务API调用层
│   ├── tenant-rbac/          # 租户权限、授权、配额
│   └── db/                   # 数据库模型
├── web-admin/                # 全可视化后台
├── runtime/
│   ├── chromium/win/mac/linux # 三端独立Chromium内核
│   └── session-cache/        # 沙箱会话临时目录
└── config/                   # 全局流程模板配置
```

## 4\. 核心能力规范（系统全局固定规则）

### 4\.1 沙箱会话规则（商用防关联核心）

- 每任务独立 Chromium 进程、独立 userDataDir、独立代理IP、独立指纹

- 支持 **sessionCache 会话复用**

- 会话存活期内：只登录一次，多次任务复用Cookie登录态

- 会话超时/代理切换/任务异常 → 自动销毁重建新会话重登

- 任务结束自动清空缓存、localStorage、indexDB

### 4\.2 WZWS 固定绕过能力（全部内置、开箱即用）

AI Agent必须完整集成，不可删减：

- 定制Chromium移除cdc\_全局变量

- 禁用AutomationControlled

- webdriver=undefined 修复

- 完整Stealth指纹抹平（Canvas/WebGL/字体/音频/插件）

- 真人贝塞尔鼠标轨迹、随机停顿、随机输入间隔

- 完整标准请求头、JA3真人指纹对齐

- 页面自然滚动、前置浏览行为、杜绝直跳

### 4\.3 流程模型固定结构（登录块 \+ 多场景块）

一套流程模板包含：

1. 全局登录配置（可开启会话缓存）

2. 多场景数组（scene1、scene2、可无限新增）

3. 每个场景独立：跳转地址、页面校验、表单映射、提交规则

## 5\. 预留【后端业务API】标准接口（重点）

系统**完全解耦业务数据**，所有填报数据全部从外部业务API拉取。

AI Agent 必须预留以下通用调用层，不写死任何业务字段。

### 5\.1 业务API统一配置项（后台可视化配置）

- API 请求地址

- 请求方式 GET/POST

- 请求头（Token、Content\-Type 等）

- 请求参数

- 数据列表字段路径（如 data\.list）

- 分页配置、批量拉取数量

### 5\.2 系统内部调用伪代码（固定）

```js
// 1. 调用外部业务API
const batchData = await apiClient.fetchBusinessData();

// 2. 循环遍历多用户数据
for (const item of batchData) {
  // 3. 单条数据执行对应场景表单提交
  await sceneRunner.run(sceneId, item);
}

```

### 5\.3 字段映射机制（核心解耦）

后台配置：**页面选择器 \<===\> 业务API字段Key**

示例：input\[name=phone\] \<===\> phone

实现：任意业务字段、任意表单，无需改代码，后台配置即可适配。

## 6\. 递进式分步开发任务（AI Agent 按 1→6 顺序开发）

**必须严格按顺序迭代，每阶段完成自测后进入下一阶段**

### 阶段1：基础架构与三端运行环境搭建

- 初始化 Tauri 三端桌面项目

- 集成 Playwright\-Core，内置三端 Chromium 资源目录

- 搭建 NestJS 本地服务、Redis、PostgreSQL 基础环境

- 实现：Windows/macOS/Ubuntu 均可启动基础程序

### 阶段2：沙箱引擎 \+ WZWS全套防风控底层实现

- 独立多进程 Chromium 启动、销毁、隔离

- 完整 stealth 指纹抹平、cdc 变量删除、webdriver 隐藏

- 真人鼠标、键盘、延时、浏览行为仿真

- 单会话独立代理、独立存储、进程隔离

- 会话缓存复用机制开发（sessionCache）

### 阶段3：流程模板引擎开发（登录复用 \+ 多场景）

- 开发模板解析器：登录块 \+ 多场景块解析

- 实现：首次登录、后续自动跳过登录

- 实现：scene1 / scene2 场景切换跳转

- 实现：表单字段自动映射填充 \+ 提交

- 步骤等待、元素等待、失败重试机制

### 阶段4：外部业务API接入层开发（预留通用接口）

- 开发可视化API配置页面

- 开发通用HTTP请求客户端

- 自动批量拉取多用户数据集

- 数据自动映射表单执行

- 循环执行引擎开发

### 阶段5：任务队列 \+ 调度系统开发

- BullMQ 任务队列封装

- 支持手动触发、批量触发、定时触发

- 租户并发配额限制

- 任务状态管理：排队/运行/成功/失败

- 失败自动重试、沙箱重建机制

### 阶段6：全可视化后台 \+ 租户授权系统

- 租户管理、角色权限、API Token授权

- 流程模板可视化配置页

- 业务API数据源可视化配置页

- 任务可视化大盘、日志、截图、执行记录

- 批量重跑、任务终止、报表导出

## 7\. 完整数据库表设计（最终版）

### 7\.1 tenant 租户表

id、name、status、concurrent\_limit、expire\_time、create\_time

### 7\.2 rpa\_process 流程模板表

id、name、session\_cache\_enable、session\_ttl、delay\_mode、login\_config、scene\_list、tenant\_id、status

### 7\.3 api\_data\_source 业务API数据源表

id、name、method、url、headers、params、data\_path、tenant\_id

### 7\.4 rpa\_task 任务调度表

id、process\_id、scene\_id、api\_source\_id、task\_data、status、progress、result、error\_msg、start\_time、end\_time、tenant\_id

### 7\.5 sandbox\_session 沙箱会话缓存表

id、session\_dir、proxy\_ip、tenant\_id、expire\_time、status

### 7\.6 user / role / permission 权限表

标准RBAC五表结构

## 8\. 标准流程模板JSON Schema（最终固定）

```json
{
  "processName": "",
  "delayMode": "fast/balance/high_sim",
  "sessionCacheEnable": true,
  "sessionCacheTtl": 3600,
  "loginBlock": {
    "loginUrl": "",
    "usernameLocator": "",
    "passwordLocator": "",
    "submitBtn": "",
    "loginSuccessVerify": ""
  },
  "sceneList": [
    {
      "sceneId": "scene1",
      "sceneName": "",
      "jumpUrl": "",
      "jumpVerify": "",
      "formFields": [
        {"locator": "", "dataKey": ""}
      ],
      "submitBtn": "",
      "successTip": ""
    },
    {
      "sceneId": "scene2",
      "sceneName": "",
      "jumpUrl": "",
      "jumpVerify": "",
      "formFields": [
        {"locator": "", "dataKey": ""}
      ],
      "submitBtn": "",
      "successTip": ""
    }
  ]
}
```

## 9\. 最终验收标准（AI Agent开发完成必须全部通过）

- ✅ 三端 Windows/macOS/Ubuntu 均可独立运行桌面客户端

- ✅ 首次任务自动登录，后续任务复用会话不重复登录

- ✅ 可自由切换场景1、场景2执行表单填报

- ✅ 可在后台配置外部业务API，自动批量拉取多用户数据循环执行

- ✅ 全套WZWS防风控、真人仿真、无机器人特征

- ✅ 多租户隔离、独立沙箱、独立代理、防关联

- ✅ 全程Web可视化配置、调度、监控、日志

- ✅ 任务队列并发控制、失败重试、自愈、会话销毁清理

- ✅ 无任何Chrome扩展、无录制功能、架构极简稳定

## 10\. 开发红线（AI Agent 禁止触碰）

- 禁止新增录制功能

- 禁止新增Chrome扩展相关代码

- 禁止做拖拽RPA画布

- 禁止修改多进程沙箱隔离架构

- 禁止删减WZWS指纹与行为仿真能力

> （注：部分内容可能由 AI 生成）
